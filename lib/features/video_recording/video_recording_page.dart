import 'package:flutter/material.dart';

/// Quay video tình huống — quay & lưu local (package `camera`), gửi chuyên
/// gia theo yêu cầu (mô phỏng trong phạm vi 1 thiết bị cho bản demo).
class VideoRecordingPage extends StatelessWidget {
  const VideoRecordingPage({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
