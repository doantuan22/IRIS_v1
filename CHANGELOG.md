# Changelog

Định dạng theo [Keep a Changelog](https://keepachangelog.com/vi/1.1.0/),
dự án tuân theo [Semantic Versioning](https://semver.org/) (xem
`docs/versioning.md`).

## [Unreleased]

### Changed
- Chuẩn hóa cấu trúc repo: thêm `docs/` (kiến trúc, ADR, versioning),
  `CHANGELOG.md`, `.editorconfig`, khung CI tối thiểu
  (`.github/workflows/ci.yml`), siết thêm vài rule lint.
- Xóa toàn bộ `test/` cũ, chuẩn bị viết lại bộ test mới theo kiến trúc sắp
  triển khai.
- Dọn dẹp file rác (ảnh chụp màn hình/log debug thủ công không còn giá trị
  tham khảo), dọn 2 thư mục asset tàn dư từ thời 9 lĩnh vực
  (`assets/videos/hanh_vi/`, `assets/videos/ung_xu/`).

## [1.0.0] — chưa gắn tag

Phiên bản hiện tại trong `pubspec.yaml` (`1.0.0+1`) — trước thời điểm bắt
đầu ghi changelog có cấu trúc. Các mốc quan trọng trước đó được ghi lại
không chính thức ở mục "Lịch sử thay đổi quan trọng" trong `README.md`.
